{
  "name": "@cronix/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./neon-schema": "./src/neon-schema.ts",
    "./neon-client": "./src/neon-client.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:run-migrate": "bun run src/migrate.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.14",
    "@neondatabase/serverless": "^0.10.0",
    "drizzle-orm": "^0.38"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30",
    "typescript": "^5"
  }
}
