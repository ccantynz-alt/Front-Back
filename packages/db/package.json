{
  "name": "@back-to-the-future/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./neon": "./src/neon.ts",
    "./neon-provisioning": "./src/neon-provisioning.ts",
    "./tenant-manager": "./src/tenant-manager.ts",
    "./migrate": "./src/migrate.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test",
    "migrate": "bun run src/migrate.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@libsql/client": "^0.17",
    "@neondatabase/serverless": "1.0.2",
    "drizzle-orm": "^0.45"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31",
    "typescript": "^6",
    "@types/bun": "^1.2.0"
  }
}
