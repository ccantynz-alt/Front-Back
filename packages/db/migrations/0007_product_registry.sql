-- Placeholder migration: referenced by meta/_journal.json but not yet authored.
-- Intentionally a no-op so drizzle's migrator can traverse the journal without
-- failing. Replace with real DDL when the product registry feature is built.
SELECT 1;
