pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "*.{ts,tsx,js,jsx,json,jsonc,css}"
      run: bunx biome check --staged --no-errors-on-unmatched {staged_files}
      stage_fixed: true
