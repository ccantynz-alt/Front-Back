# ── LGTM stack environment ─────────────────────────────────────────
# Copy to .env and edit. Never commit .env.

# Grafana admin credentials (MUST rotate before exposing publicly)
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=change_me_now

# Public URL Grafana is served under (via Caddy reverse proxy)
GF_ROOT_URL=https://grafana.crontech.nz
