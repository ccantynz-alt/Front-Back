[install]
# Exact versions for reproducible builds
exact = true

[install.lockfile]
# Always save the lockfile
save = true

[test]
# E2E visual/a11y/link coverage is provided by GateTest (see package.json: test:e2e).
# No per-directory exclusions required — GateTest runs against the dev server.
